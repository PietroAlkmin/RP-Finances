/**
 * Configuração do PostCSS
 * 
 * PostCSS processa o CSS e aplica plugins como:
 * - Tailwind CSS (classes utilitárias)
 * - Autoprefixer (adiciona prefixos de navegador automaticamente)
 */

export default {
  plugins: {
    // Tailwind CSS - Framework de classes utilitárias
    tailwindcss: {},
    
    // Autoprefixer - Adiciona prefixos CSS para compatibilidade
    autoprefixer: {},
  },
}
